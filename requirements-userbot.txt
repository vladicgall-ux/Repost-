# Зависимости для userbot.py (вариант на пользовательском аккаунте).
# Для bot-версии (main.py) ставьте requirements.txt.
telethon==1.45.0

# Нужен только для входа в аккаунт прямо в чате с ботом (хостинг без терминала).
# Если получаете сессию через `python userbot.py --login`, строку можно удалить.
aiogram==3.15.0

# Необязательно: ускоряет шифрование в разы на больших медиа.
# Если на хостинге нет компилятора — просто удалите эту строку, всё работает и без неё.
cryptg==0.5.0.post0
