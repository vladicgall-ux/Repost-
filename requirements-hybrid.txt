# Зависимости для hybrid.py: userbot читает источник, бот публикует.
# hybrid.py переиспользует resolve_source из userbot.py — держите оба файла рядом.
telethon==1.45.0
aiohttp==3.10.11

# Необязательно: ускоряет шифрование на больших медиа.
# Нет компилятора на хостинге — просто удалите строку.
cryptg==0.5.0.post0
